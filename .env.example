# Frontend configuration (Vite)
# Point to your backend base URL (no trailing slash). Example:
# VITE_API_BASE_URL=http://localhost/deliberation
# Or if running PHP built-in server at port 8000:
# VITE_API_BASE_URL=http://localhost:8000/backend
VITE_API_BASE_URL=

# Backend configuration (PHP)
# Database connection settings
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=deliberation
DB_USER=root
DB_PASS=